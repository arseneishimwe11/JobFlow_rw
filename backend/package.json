{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "packageManager": "bun",
  "scripts": {
    "build": "cd ../frontend && bun install && vite build --outDir=../backend/frontend/dist"
  },
  "dependencies": {
    "encore.dev": "^1.48.12",
    "node-cron": "^4.2.1",
    "puppeteer": "^24.16.0"
  },
  "devDependencies": {
    "@types/node": "^24.2.0",
    "typescript": "^5.8.3"
  }
}
